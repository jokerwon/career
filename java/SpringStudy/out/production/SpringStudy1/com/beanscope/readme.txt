## Spring Bean的作用域

- 单例(singleton): 在整个容器的生命周期中只创建 bean 的一个实例。在初始化 IOC容器的时候创建对象，之后每次都引用该对象。
- 原型(prototype): 每次注入或通过上下文获取的时候都会创建一个新的 bean实例。初始化容器时不创建 bean实例。
- 会话(session): 在 web应用中，为每个会话创建一个实例。
- 请求(request): 在 web应用中，为每个请求创建一个实例。